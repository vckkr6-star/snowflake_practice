version: 2

sources:
  - name: raw
    database: dbt_db
    schema: dbt_schema
    

    tables:
      - name: raw_customers_2
        config:
          loaded_at_field: loaded_at
          freshness:
            warn_after:
              count: 1
              period: day

            error_after:
              count: 2
              period: day